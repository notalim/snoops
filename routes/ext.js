import { Router } from "express";
const router = Router();
import { extData } from "../data/index.js";
import * as validation from "../validation.js";

// TODO: Extension Routes to users and adoption centers

export default router;